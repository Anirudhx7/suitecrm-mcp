global:
  resolve_timeout: 5m
  # smtp_smarthost: 'smtp.gmail.com:587'
  # smtp_from: 'alerts@yourdomain.com'
  # smtp_auth_username: 'alerts@yourdomain.com'
  # smtp_auth_password: 'your-app-password'
  # smtp_require_tls: true

route:
  group_by: ['alertname', 'entity']
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 4h
  receiver: 'null'

  # Uncomment and configure a receiver below to route alerts to email/Slack/PagerDuty.
  # routes:
  #   - receiver: 'email'
  #     match:
  #       severity: critical

receivers:
  - name: 'null'

  # - name: 'email'
  #   email_configs:
  #     - to: 'you@yourdomain.com'
  #       send_resolved: true
  #       headers:
  #         Subject: '[{{ .Status | toUpper }}] {{ .GroupLabels.alertname }} — {{ .GroupLabels.entity }}'
  #       html: |
  #         <b>{{ .GroupLabels.alertname }}</b><br>
  #         <b>Entity:</b> {{ .GroupLabels.entity }}<br>
  #         <b>Status:</b> {{ .Status }}<br><br>
  #         {{ range .Alerts }}
  #         <b>Summary:</b> {{ .Annotations.summary }}<br>
  #         <b>Detail:</b> {{ .Annotations.description }}<br>
  #         {{ end }}

  # - name: 'slack'
  #   slack_configs:
  #     - api_url: 'https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK'
  #       channel: '#alerts'
  #       title: '{{ .GroupLabels.alertname }}'
  #       text: '{{ range .Alerts }}{{ .Annotations.description }}{{ end }}'

inhibit_rules:
  - source_match:
      severity: 'critical'
    target_match:
      severity: 'warning'
    equal: ['entity']
